/**
 * The lido-local-devnet chain id. Not part of CHAINS in @lido-nestjs/constants,
 * so every per-network map that supports the devnet keys off this constant.
 */
export const LOCAL_DEVNET_CHAIN_ID = 32382;
