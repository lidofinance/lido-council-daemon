export const MAIN_PROVIDER_REQUEST_TIMEOUT = 15 * 60 * 1000; // 15 minutes
// Don't reset provider URL selection, only on error
export const PROVIDER_RESET_INTERVAL = 60 * 60 * 1000 * 24 * 10; // 10 days
